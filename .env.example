# Copy to .env to override docker-compose defaults.
# Secret used to sign JWTs — change this for any real deployment.
JWT_SECRET=dev_secret_change_me
# Host port the web app is published on (maps to the web container's port 80).
WEB_PORT=8080
